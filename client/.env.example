# 開発時は Vite の proxy (/ws) 経由で wrangler dev に接続するため未設定でOK
# 同一オリジンデプロイ(Cloudflare Workers + assets)では本番でも設定不要
# クライアントとWorkerを別ドメインに分けて運用する場合のみ設定する
# VITE_SERVER_URL=https://your-worker.example.workers.dev
